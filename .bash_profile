
# added by Anaconda3 4.4.0 installer
export PATH="$HOME/anaconda3/bin:$PATH"

## @NOTE: overrides 'cd' so it always pauses for half a second when cd-ing
# source /usr/local/opt/autoenv/activate.sh

source ~/.bashrc
